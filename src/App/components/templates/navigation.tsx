import React, { PropsWithChildren } from "react";
import { Wraper } from "../atoms/styled-components";

const navigation = (props:PropsWithChildren<{}>)=>{
    return <Wraper>
        
    </Wraper>
}

export default navigation;